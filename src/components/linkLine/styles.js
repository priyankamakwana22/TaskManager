import Colors from '../../themes/Colors';
import FontSize from '../../themes/FontSize';

const styles = {
  link_view: {flexDirection: 'row', gap: 8, marginTop: 20},
  link_text1: {color: Colors.BLACK, fontSize: FontSize.FONT_LINK},
  link_text2: {
    color: Colors.LINK,
    fontSize: FontSize.FONT_LINK,
    fontWeight: '600',
  },
};

export default styles;
